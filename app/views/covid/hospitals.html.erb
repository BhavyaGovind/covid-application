<%= render :partial => 'home' %>
<div class="center-block fix-width fix-height scroll-inner">
  <table class="table table-striped">
    <tr>
      <% headings = ["Clinic Name","Address","Suburb","Instruction"] %>
      <% headings.each do |header| %>
      <th><%= header %></th>
      <% end %>
    </tr>

    <% @records.each do |row| %>
      <tr>
        <% @table_headers.each do |header| %>
          <td><%= row[header] %></td>
        <% end %>
      </tr>
    <% end %>
  </table>
</div>
